import React from "react";
import './Title.css'

const Title = () => {
  return (<section className="headers">
  <header className="title">weather.</header>
  <header className="subtitle">the only weather app you need.</header>
  </section>
  );
};

export default Title;
